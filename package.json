{
  "name": "switchboard-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Cost-aware System One/System Two AI orchestration runtime using Jev for routing and frontier models for execution.",
  "type": "module",
  "scripts": {
    "start": "node src/cli.mjs",
    "demo": "node src/cli.mjs --dry-run \"Analyze this TypeScript repo, fix the failing tests, and explain the patch\"",
    "test": "node --test tests/*.test.mjs",
    "benchmark": "node scripts/benchmark.mjs"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^1.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
